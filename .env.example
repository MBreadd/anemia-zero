PORT=3000
APP_BASE_URL=http://localhost:3000

# PostgreSQL (local / Docker)
DB_HOST=localhost
DB_PORT=5432
DB_USER=anemia_zero
DB_PASSWORD=anemia_zero
DB_NAME=anemia_zero

# Alternativa para Vercel / hosting serverless: si defines DATABASE_URL,
# tiene prioridad sobre las variables DB_* de arriba (Neon, Supabase, Vercel Postgres...)
# DATABASE_URL=postgres://usuario:password@host:5432/basededatos

# Sesiones (login de usuarios)
SESSION_SECRET=pon-aqui-otro-secreto-largo
